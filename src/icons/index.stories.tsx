import React from 'react';

import { CaretDown } from './';

export default {
  component: CaretDown,
  title: 'Icons',
};

const Template = () => (
  <>
    <CaretDown />
  </>
);

export const Default = Template.bind({});
