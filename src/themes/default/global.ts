import { primaryFont } from './_fonts';

const global = {
  color: '#222222',
  htmlFontFamily: primaryFont.family,
  htmlFontSize: '10px',
  htmlFontWeight: primaryFont.weight,
};

export default global;
