import { text } from './_colors';
import { primaryFont } from './_fonts';

const header = {
  color: text.color100,
  fontFamily: primaryFont.family,
  fontWeight: primaryFont.weight,
  _bold: {
    fontWeight: 500,
  },
};

export default header;
